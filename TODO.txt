FFT and audio analysis needs reworking
- Implement STFT

Fix error handling system
- How? What's wrong with it?
X Hot reloading shaders
X Change shader uniforms on the fly via GUI for preset shaders such as perlin noise etc...
X Have custom shader, then a bunch of presets built on the same system or have preset shaders with associated c++ classes?
X - Custom shader and presets would require some way of communicating to the GUI what uniforms
